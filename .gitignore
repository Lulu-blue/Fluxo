# Backup local da migração de anexos (migracao/migrar_anexos.mjs).
# Contém ARs e multas com dados pessoais de contribuintes: nunca pode ir para o GitHub.
migracao/

# Cópias completas do banco feitas com pg_dump
*.dump
